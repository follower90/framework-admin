<form role="form" action="/admin/<?= $vars['controller']; ?>/save">
	<?php
		foreach ($vars['fields'] as $field) {
			echo $field;
		}
	?>
	<div class="form-group">
		<input type="submit" class="btn btn-default" value="<? __('Save'); ?>" />
		<input type="button" class="btn btn-cancel" onclick="location.href='/admin/<?= $vars['controller']; ?>';" value="<? __('Cancel'); ?>" />
	</div>
</form>
