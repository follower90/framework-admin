<h1><? __('Cart'); ?></h1>

<div class="table-responsive">
	<table class="table table-hover">
		<thead>
			<tr>
				<th>#</th>
				<th><? __('Name') ?></th>
				<th><? __('Count') ?></th>
				<th><? __('Price') ?></th>
				<th><? __('Sum') ?></th>
				<th><? __('Actions') ?></th>
			</tr>
		</thead>
		<tbody>
		<?php $counter = 1; ?>
		<?php foreach ($vars['cart'] as $item) { ?>
			<tr>
				<td><?= $counter++; ?></td>
				<td><?= $item['product']['name']; ?></td>
				<td><input type="number" class="cart-num-<?= $item['id']; ?>" value="<?= $item['count']; ?>"></td>
				<td><?= $item['product']['price']; ?></td>
				<td><?= number_format($item['product']['price']*$item['count'], 2); ?></td>
				<td>
					<a href="/cart/remove/<?= $item['id']; ?>" onclick="">remove</a> |
					<a href="#" onclick='updateProductCount(<?= $item['id']; ?>, $(".cart-num-<?= $item['id']; ?>").val())'>update</a>
				</td>
			</tr>
		<?php } ?>
		</tbody>
	</table>

	<button class="btn btn-success" onclick="confirmOrder()"> <?__('Order'); ?></button>
</div>
